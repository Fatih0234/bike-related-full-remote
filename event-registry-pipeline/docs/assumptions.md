# Assumptions

- Python 3.11 is the baseline for compatibility across ingestion and LLM libs.
- The project root is `/Volumes/T7/event-registry-pipeline`.
- Postgres access is available via `DATABASE_URL` or split PG env vars.
- The Open311 base URL remains `https://sags-uns.stadt-koeln.de/georeport/v2`.
